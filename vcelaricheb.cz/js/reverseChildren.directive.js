// Generated by CoffeeScript 1.9.3
(function() {
  angular.module("vcelaricheb").directive("reverseChildren", function() {
    return {
      restrict: "A",
      link: function(scope, element, attrs) {
        var childrenCount, elem, myTag, skip;
        skip = 0;
        if (attrs.reverseChildren != null) {
          myTag = eval("({" + attrs.reverseChildren + "})");
          if (myTag.skip != null) {
            skip = myTag.skip + 0;
          }
        }
        elem = $(element);
        elem.children().each(function(i, ch) {
          return elem.prepend(ch);
        });
        childrenCount = elem.children().size();
        return elem.children().each(function(i, ch) {
          if (i >= childrenCount - skip) {
            return elem.prepend(ch);
          }
        });
      }
    };
  });

}).call(this);

//# sourceMappingURL=reverseChildren.directive.js.map
