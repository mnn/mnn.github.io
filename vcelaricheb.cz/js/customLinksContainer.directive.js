// Generated by CoffeeScript 1.9.3
(function() {
  angular.module("vcelaricheb").directive("customLinksContainer", function() {
    return {
      restrict: "E",
      templateUrl: "templates/customLinksContainer.html",
      controller: "CustomLinksContainerCtrl",
      scope: {
        maxColumns: "="
      },
      transclude: true,
      link: function(scope, element, attrs) {
        var defGridSize, itemsCount, wrapper;
        wrapper = $(element).find("ng-transclude");
        if ((attrs.reversed != null) || (attrs.reverse != null)) {
          wrapper.children().each(function(i, ch) {
            return wrapper.prepend(ch);
          });
        }
        wrapper.children().wrap("<li class=\"custom-links-container-item\"></li>");
        wrapper.children().appendTo(wrapper.parent());
        wrapper.remove();
        itemsCount = $(element).find("ul").children().size();
        defGridSize = 4;
        scope.gridSize = Math.min(defGridSize, itemsCount);
        if (scope.maxColumns != null) {
          return scope.gridSize = Math.min(scope.maxColumns, scope.gridSize);
        }
      }
    };
  }).controller("CustomLinksContainerCtrl", function($scope) {});

}).call(this);

//# sourceMappingURL=customLinksContainer.directive.js.map
